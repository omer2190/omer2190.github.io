# 🎛️ لوحة التحكم - Dashboard

## 📋 نظرة عامة

لوحة تحكم احترافية لإدارة محتوى الموقع الشخصي بسهولة. تعمل حالياً بنظام `localStorage` وجاهزة للربط بـ Backend مستقبلاً.

## 🔐 معلومات الدخول الافتراضية

- **اسم المستخدم:** `admin`
- **كلمة المرور:** `omer2190`

> ⚠️ **مهم:** قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول من قسم الإعدادات!

## ✨ الميزات

### 1. إدارة المشاريع

- ✅ إضافة مشاريع جديدة (عربي + إنجليزي)
- ✅ تعديل المشاريع الموجودة
- ✅ حذف المشاريع
- ✅ إدارة التقنيات والصور

### 2. المعلومات الشخصية

- ✅ تعديل الاسم والوصف
- ✅ دعم اللغتين العربية والإنجليزية

### 3. إدارة المهارات

- ✅ عرض المهارات مع نسب التقدم
- 🔜 إضافة/تعديل/حذف (قريباً)

### 4. معلومات التواصل

- ✅ تعديل البريد الإلكتروني
- ✅ روابط LinkedIn و GitHub

### 5. الإعدادات

- ✅ تغيير كلمة المرور
- ✅ تصدير البيانات (JSON)
- ✅ استيراد البيانات (JSON)
- ✅ إعادة تعيين البيانات

## 🚀 كيفية الاستخدام

### الوصول للوحة التحكم:

```
افتح: https://yourdomain.com/admin/
أو محلياً: file:///path/to/project/admin/index.html
```

### تسجيل الدخول:

1. أدخل اسم المستخدم: `admin`
2. أدخل كلمة المرور: `omer2190`
3. اختر "تذكرني" للبقاء مسجلاً لمدة 30 يوم

### إضافة مشروع جديد:

1. اذهب إلى قسم "المشاريع"
2. اضغط "إضافة مشروع جديد"
3. املأ بيانات المشروع بالعربي والإنجليزي
4. اضغط "إضافة المشروع"

### تصدير البيانات:

1. اذهب إلى "الإعدادات"
2. اضغط "تصدير البيانات (JSON)"
3. سيتم تحميل ملف JSON يحتوي على جميع بياناتك

## 📁 هيكل الملفات

```
admin/
├── index.html          # صفحة تسجيل الدخول
├── dashboard.html      # لوحة التحكم الرئيسية
├── dashboard.css       # تنسيقات Dashboard
├── dashboard.js        # المنطق البرمجي
├── auth.js            # نظام المصادقة
├── data-manager.js    # إدارة البيانات
└── README.md          # هذا الملف
```

## 💾 التخزين

### حالياً (localStorage):

- جميع البيانات محفوظة في متصفحك
- آمن ومجاني 100%
- يعمل بدون إنترنت
- **تحذير:** مسح cache المتصفح = فقدان البيانات

### احتياطات الأمان:

1. قم بتصدير البيانات بانتظام
2. احفظ نسخة احتياطية من ملف JSON
3. غيّر كلمة المرور الافتراضية

## 🔄 الربط بـ Backend (مستقبلاً)

Dashboard جاهز للربط بأي Backend! فقط عدّل `data-manager.js`:

### مثال: ربط بـ Firebase

```javascript
// في data-manager.js
async fetchFromBackend() {
  const response = await fetch('https://your-firebase-url.com/data.json');
  const data = await response.json();
  return data;
}

async syncToBackend() {
  const data = this.getData();
  await fetch('https://your-firebase-url.com/data.json', {
    method: 'PUT',
    body: JSON.stringify(data)
  });
}
```

### مثال: ربط بـ MongoDB + Node.js

```javascript
async fetchFromBackend() {
  const response = await fetch('http://localhost:3000/api/portfolio');
  return await response.json();
}

async syncToBackend() {
  await fetch('http://localhost:3000/api/portfolio', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(this.getData())
  });
}
```

## 🎨 التخصيص

### تغيير الألوان:

عدّل متغيرات CSS في `dashboard.css`:

```css
:root {
  --primary-color: #667eea; /* لون رئيسي */
  --secondary-color: #764ba2; /* لون ثانوي */
  --success-color: #43e97b; /* لون النجاح */
  --danger-color: #fa5252; /* لون الخطر */
}
```

### إضافة قسم جديد:

1. أضف رابط في `sidebar-nav`
2. أنشئ `<section>` جديد في `dashboard.html`
3. أضف الـ logic في `dashboard.js`

## 🐛 استكشاف الأخطاء

### مشكلة: لا أستطيع تسجيل الدخول

**الحل:**

1. تأكد من استخدام المعلومات الصحيحة
2. اضغط "نسيت كلمة المرور؟" لإعادة التعيين
3. امسح cache المتصفح

### مشكلة: البيانات لا تُحفظ

**الحل:**

1. تأكد من تفعيل localStorage في المتصفح
2. تحقق من وجود مساحة كافية
3. جرب متصفح آخر

### مشكلة: الصور لا تظهر

**الحل:**

1. تأكد من رفع الصور في مجلد `assets/projects/`
2. تحقق من أسماء الملفات: `p (1).png`, `p (2).png`
3. تأكد من `imageCount` صحيح

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي:** قم بتصدير البيانات بانتظام
2. **الأمان:** غيّر كلمة المرور فوراً
3. **الصور:** يجب رفع الصور يدوياً في `assets/projects/`
4. **التحديثات:** سيتم إضافة ميزات جديدة قريباً

## 🔮 الميزات القادمة

- [ ] محرر نصوص WYSIWYG
- [ ] رفع صور مباشر من Dashboard
- [ ] إحصائيات متقدمة
- [ ] دعم ثيمات متعددة
- [ ] تطبيق موبايل للإدارة
- [ ] نظام إشعارات
- [ ] تاريخ التعديلات (History)

## 📞 الدعم

إذا واجهت أي مشكلة:

1. راجع قسم "استكشاف الأخطاء"
2. افحص Console في المتصفح
3. تأكد من تحديث المتصفح

---

**تم التطوير بواسطة:** GitHub Copilot  
**التاريخ:** ديسمبر 2025  
**الإصدار:** 1.0.0

🎉 **مبروك! Dashboard جاهز للاستخدام!**
